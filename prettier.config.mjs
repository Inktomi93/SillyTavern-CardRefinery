/** @type {import('prettier').Config} */
const config = {
    useTabs: false,
    tabWidth: 4,
    singleQuote: true,
    semi: true,
    trailingComma: 'all',
    bracketSpacing: true,
    endOfLine: 'lf',
};

export default config;
